using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public  class DamageText : CombatText
{

    // Start is called before the first frame update
    void Start()
    { 
        color = Color.red;
        fontSize = 20;
    }

    // Update is called once per frame
    void Update()
    {

    }

}
