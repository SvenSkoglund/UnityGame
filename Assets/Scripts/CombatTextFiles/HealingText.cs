using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public  class HealingText : CombatText
{

    // Start is called before the first frame update
    void Start()
    { 
        color = Color.green;

    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
