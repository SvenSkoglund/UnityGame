using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public  class StatusText : CombatText
{

    // Start is called before the first frame update
    void Start()
    { 
        color = Color.gray;
        fontSize =200;

    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
